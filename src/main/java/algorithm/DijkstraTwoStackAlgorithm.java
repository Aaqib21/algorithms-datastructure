package algorithm;

/*
* VALUE:  push onto the value stack
* OPERATOR: push onto the operator stack
* LEFT ( : ignore
* RIGHT ) : pop operator and two values; push the result of applying that operator to those values onto the value stack
*
* implement only + and *
* e.g (1+((2+3)*(4*5)))
* */

public class DijkstraTwoStackAlgorithm {
}
